[build]
builder = "NIXPACKS"

[deploy]
# Runs at 03:00 and 15:00 UTC, shortly after CMEMS twice-daily model runs
cronSchedule = "0 3,15 * * *"
startCommand = "python fetcher.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
